<p id="notice"><%= notice %></p>

<h1>Listing Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Url</th>
      <th>Quantity</th>
      <th>Amount</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr class="editbox">
        <td><%= product.name %></td>
        <td><%= image_tag(product.url, :alt => product.name, width:'100') %></td>
        <td><%= product.quantity %></td>
        <td><span>$</span><%= product.amount %></td>
        <td><%= product.category.id %></td>
<<<<<<< HEAD:app/views/admin/products/index.html.erb
        <td><i class="material-icons">search</i><%= link_to 'Show', admin_product_path(product) %></td>
        <td><i class="material-icons">edit</i><%= link_to 'Edit', edit_admin_product_path(product)%></td>
        <td><i class="material-icons">delete</i><%= link_to 'Destroy', admin_product_path(product), method: :delete, data: { confirm: 'Are you sure?' } %></td>
=======
        <td><%= link_to 'Show', lab_admin_product_path(product) %></td>
        <td><%= link_to 'Edit', edit_lab_admin_product_path(product) %></td>
        <td><%= link_to 'Destroy', lab_admin_product_path(product), method: :delete, data: { confirm: 'Are you sure?' } %></td>
>>>>>>> f978a78534f784694df8826be6de966e58fad2fc:app/views/lab/admin/products/index.html.erb
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @products %>

<br>

<%= link_to 'New Product', new_lab_admin_product_path %>

<%= will_paginate @products %>
